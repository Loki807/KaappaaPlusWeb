export interface Tenant {
  id: string;
  name: string;
  code: string;
  city: string;
  stateOrDistrict: string;
  postalCode: string;
  contactNumber: string;
  service: string;
  email: string;
  role: string;
  status: string;
}